services:
  app:
    build:
      context: .
      dockerfile: ./Dockerfile
      target: app
    ports:
      - 8080:8080
    restart: unless-stopped
    volumes:
      - /Users/nicolasboyer/Documents/IR:/data/storage
      - db:/data/db
    env_file: .env
    environment:
      - DATABASE_URL=file:/data/db/penombre.sqlite
      - LOG_LEVEL=debug
      - STORAGE_PATH=/data/storage
      - SIMPLE_MODE=true
      - ORIGIN=http://localhost:8080

volumes:
  db: {}
