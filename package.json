{
  "name": "@opendrive/root",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "prepare": "husky",
    "test": "playwright test",
    "dev": "node --env-file .env concurrently.config.mjs",
    "ci:tasks": "node concurrently.config.ci.mjs",
    "build": "pnpm -C packages/ui run build && go -C packages/api build"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@playwright/test": "^1.55.0",
    "@types/node": "^24.3.0",
    "concurrently": "^9.2.1",
    "husky": "^9.1.7",
    "lint-staged": "^16.1.6"
  },
  "dependencies": {
    "dotenv": "^17.2.2"
  }
}
